package store

import (
	"testing"
)

func TestNew(t *testing.T) {
	//logs.Init("trace")
	//db, err := New[string, shopify.Order]("orders.json")
	//assert.NoError(t, err)
	//defer db.Close()
	//
	//fmt.Println(db)
}
